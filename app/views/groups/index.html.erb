<h1>Groups</h1>

<% @groups.each do |group| %>
  <div>
    <a href= <%= "/groups/#{group.id}" %>><strong><%= "#{group.class}: "%></strong><%= group.name %></a>
    <p><%= group.description %><p>
  </div>
<% end %>

<br />

<%= link_to 'New Group', new_group_path %>
